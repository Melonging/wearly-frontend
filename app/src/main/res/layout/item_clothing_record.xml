<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="120dp"
    android:layout_height="120dp">

    <!-- 옷 이미지 -->
    <ImageView
        android:id="@+id/clothingIv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <View
        android:id="@+id/selectedOverlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/clothingIv"
        app:layout_constraintBottom_toBottomOf="@id/clothingIv"
        app:layout_constraintStart_toStartOf="@id/clothingIv"
        app:layout_constraintEnd_toEndOf="@id/clothingIv" />

    <ImageView
        android:id="@+id/checkIv"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_margin="8dp"
        android:src="@drawable/ic_codidiary_check_white"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/clothingIv"
        app:layout_constraintEnd_toEndOf="@id/clothingIv" />

</androidx.constraintlayout.widget.ConstraintLayout>